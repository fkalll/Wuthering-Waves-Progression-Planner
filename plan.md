【目标】：
- 优化 `wuwa_planner` 当前 Tkinter 界面的整体卡片布局与配色，在不更换 GUI 框架的前提下，将现有偏系统原生的浅灰界面调整为更统一的深蓝灰游戏化风格：应用背景、一级卡片、二级卡片、边框、正文、弱化文字和强调色形成清晰层级，同时保证材料数量、提示文字和表单控件有足够对比度。
- 重新组织顶部、角色侧栏、角色详情、养成材料和配装建议区域的间距与卡片层级；角色列表、计算条件、汇总信息、材料条目及配装字段使用一致的容器样式、选中反馈和悬停反馈，并继续支持当前最小窗口尺寸及缩放。
- 将顶部“伤害属性”文字下拉框替换为“全部 + 六种伤害属性”的横向图标按钮组。按钮本体只显示图标，不显示“全部、冷凝、热熔、导电、气动、衍射、湮灭”全文；鼠标移入时显示对应简体中文提示，选中项通过属性色、描边和背景状态明确区分。
- 属性图标使用 Pillow 在运行时绘制的代码内置几何图形和属性色，不新增或下载游戏素材；保持 macOS 与 Windows 打包兼容，并继续与文字搜索组合筛选。

【规则】：
- 本轮预计修改或新增的文件仅限：
  - `plan.md`：记录本次经确认的界面优化范围。
  - `wuwa_planner/main.py`：集中调整主题色、控件样式、卡片容器、角色与材料列表视觉状态；新增属性图标生成与缓存、图标按钮组、选中状态切换和中文悬浮提示组件。
  - `wuwa_planner/test_ui.py`：新增不创建主窗口的轻量界面配置测试，检查属性图标配置与七个筛选值完整对应、中文提示完整、图标可生成且状态尺寸正确。
  - `wuwa_planner/README.md`：将筛选说明更新为图标筛选和中文悬浮提示，并补充新的界面测试覆盖说明。
- 采用深蓝灰背景、稍亮卡片、蓝紫主强调色与六属性独立颜色；不依赖系统深色模式，不引入自定义字体，并确保正文与背景具有清晰可读的明暗差。Tkinter 不原生支持真正的 CSS 圆角，本轮通过无凸起边框、分层底色、细描边、统一内边距和卡片间距实现现代卡片感，不承诺网页级阴影或动画。
- 顶部保留“伤害属性”分组标题，但七个筛选选项仅显示图标；“全部”使用中性总览图标，六属性分别使用容易区分的几何符号。中文名称只出现在鼠标悬浮提示、状态提示和必要的无障碍/内部文本中，不直接铺满筛选栏。
- 悬浮提示使用独立、可复用的 Tkinter 小组件：鼠标进入后在控件附近显示中文，移出、点击、窗口失焦或控件销毁时关闭；不得残留多个提示窗口，也不得抢占输入焦点。图标按钮必须可以点击，并保留键盘聚焦与回车/空格触发能力。
- 点击属性图标时继续调用现有 `search_characters`，保持“属性筛选与文字搜索取交集”；点击已选属性不改变筛选语义，“清空”仍恢复“全部”并清除搜索词。无结果、切换角色以及当前角色不在新结果中的行为保持不变。
- 保留现有 56 个战斗形态、角色/材料/声骸本地图片、中文名称、等级与技能计算、技能树开关、配装内容、离线运行、数据自检和图片缺失占位逻辑。不得修改 `data/*.json`、`assets/**`、`calculator.py`、`data_loader.py`、依赖或打包配置。
- 不新增第三方依赖，不联网下载图标，不切换到 Qt、Web、Flutter 或 Electron；不修改 `calculator`、`glances` 等其他项目，不自动重新打包 App/EXE/ZIP，不提交、推送、发布或部署。
- 若实现中发现必须新增图片资源、依赖、数据字段或修改上述范围外文件，停止实施，先更新 `plan.md` 并重新等待“执行计划”。

【测试】：
- 执行 `PYTHONPYCACHEPREFIX=/private/tmp/python-project-pycache .venv/bin/python -m py_compile wuwa_planner/main.py wuwa_planner/test_ui.py`，验证界面代码与新增测试语法正确，且不在项目内生成新的缓存文件。
- 执行 `PYTHONPYCACHEPREFIX=/private/tmp/python-project-pycache .venv/bin/python -m unittest discover -s wuwa_planner -p 'test_*.py' -v`，确认新增界面配置测试和既有数据、搜索、资源、等级、技能及配装测试全部通过。
- 新增自动化测试覆盖七个筛选值与图标配置一一对应、提示文字为正确的简体中文、属性配色完整、默认/悬停/选中图标可生成且尺寸和图像模式符合预期；现有搜索测试继续覆盖属性与文字搜索交集。
- 执行 `.venv/bin/python wuwa_planner/main.py --check-data`，确认视觉重构没有影响 56 个战斗形态、147 种材料、226 张本地图片的离线加载和数据自检。
- 在获准的 macOS 桌面环境启动 `wuwa_planner/main.py` 并截取新版界面；人工检查默认 1260×820 与最小 980×680 两种尺寸下，顶部筛选按钮不重叠，左右卡片可用，材料与配装内容可滚动，文本不被背景吞没。
- 人工逐一悬浮“全部、冷凝、热熔、导电、气动、衍射、湮灭”七个图标，确认中文提示位置正确且移开后消失；逐一点击六属性，确认选中样式、角色数量和角色属性正确；再验证搜索词与属性交集、无结果提示及“清空”恢复全部。
- 人工检查角色列表选中/悬停状态、角色详情属性徽标、计算控件、汇总卡、材料条目、配装声骸卡和页签在新主题下的视觉一致性，并正常关闭窗口。
- 不执行 App、EXE 或 ZIP 重新打包；不测试运行时联网、数据库、OCR、武器养成或伤害模拟，因为这些不在本轮修改范围内。
- 最终验收条件：界面完成统一卡片化与深蓝灰配色；伤害属性筛选改为七个无全文字图标按钮并具有正确中文悬浮提示；筛选、搜索、计算和离线数据行为不回退；语法检查、全部单元测试、数据自检及桌面人工验证通过。
